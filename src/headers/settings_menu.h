#ifndef SETTINGS_MENU_H
#define SETTINGS_MENU_H

/*
    The "SettingsHandler" is a standalone part of
    the program, it handles the whole settings
    page and it also applies the changes required 
    from the user on its own.
*/
void SettingsHandler();

#endif